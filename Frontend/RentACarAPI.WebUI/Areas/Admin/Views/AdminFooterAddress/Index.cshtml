@model List<ResultFooterAddressDto>

@{
	ViewData["Title"] = "Index";
	Layout = "~/Views/AdminLayout/Index.cshtml";

}

<div class="row">
	<div class="col-xl-12">
		<div class="card">
			<div class="card-body">
				<h4 class="card-title">Footer Address</h4>
				<p class="card-subtitle mb-4">
					Use table to view car footeraddresss, delete car featuress and navigating to create and update forms.
				</p>
				<div class="table-responsive">
					@if (Model.Any())
					{
						<table class="table mb-0">
							<thead>
								<tr>
									<th>Description</th>
									<th>Address</th>
									<th>Phone</th>
									<th>Email</th>
									<th>Update</th>
									<th>Delete</th>
								</tr>
							</thead>
							<tbody>
								@foreach (var item in Model)
								{
									<tr>
										<td>@item.Description</td>
										<td>@item.Address
										<td>@item.Phone</td>
										<td>@item.Email</td>
										<td>
											<a href="/Admin/AdminFooterAddress/UpdateFooterAddress/@item.FooterAddressID" type="button" class="btn btn-outline-warning waves-effect waves-light">Update</a>
										</td>
										<td>
											<a href="/Admin/AdminFooterAddress/RemoveFooterAddress/@item.FooterAddressID" type="button" class="btn btn-outline-danger waves-effect waves-light">Delete</a>
										</td>
									</tr>
								}
							</tbody>
						</table>
					}
					else
					{
						<div class="card text-white bg-warning">
							<div class="card-body">
								<blockquote class="card-bodyquote mb-0">
									<p>
										There are no foote raddresses here yet.
									</p>
									<footer class="blockquote-footer text-white-50">
										No Data in <cite title="Source Title">Footer Addresss</cite>
									</footer>
								</blockquote>
							</div>
						</div>
					}

					<a href="/Admin/AdminFooterAddress/CreateFooterAddress" type="button" class="btn btn-outline-secondary waves-effect waves-light">New Footer Address</a>

				</div>

			</div>

		</div>

	</div>
</div>







